public class Key{
    String usage;

    public Key(String usage){ 
        // coorisponding door attribute is in the door class, not sure if this will work in the long run though
        this.usage = usage;
    }
}
